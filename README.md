# HideLockWidgets

LSPosed lockscreen widgets + depth wallpaper preview.

Важно: для live widgets один раз выдать grant:

```sh
su -c 'appwidget grantbind --package com.android.systemui --user 0'
```
